We have matrices and vectors which can interract with one another.
We are using linkedlist to store data.
We can search for a vector, print every vector or find larges, smallest one.
We can also find min or max column of a matrix.
We can pad a vector and matrix.
This program designed to work with a .txt file to get matrix and vector informations.